package com.currently.currently_backend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class CurrentlyBackendApplication {

	// Application entry point: starts the Currently Spring Boot backend API.
	public static void main(String[] args) {
		SpringApplication.run(CurrentlyBackendApplication.class, args);
	}

}
